# プロジェクト設定テンプレート（Eclipse / Java）

リポジトリを clone した直後、`.project` や `.classpath` が無いときにここからコピーして使います。

- **使い方**: リポジトリルートで `SETUP.md` の手順に従い、このフォルダ内のファイルをルートにコピーしてください。
- **中身**: `.project.example`, `.classpath.example`, `.settings.example/`（Eclipse / VS Code Java 用）
