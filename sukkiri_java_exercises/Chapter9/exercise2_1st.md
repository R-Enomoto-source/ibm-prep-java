答えの導出過程
name:アサカ　hp:25 mp:5
heal(baseHp);　はtに入っているThiefインスタンスを指定していないので変化なし。
と思ったが、heal(baseHp);は8行目のものを使っていると思われる。返し値がないが、hpに入ってるのはbasehpであるはずで、なので+10されていると思われる。なので、
name:アサカ　hp:25 mp:5
basehp:35
よって、表示結果は
35:25
heal(t);　はtに入っているThiefインスタンスを指定してるので、
name:アサカ　hp:35 mp:5
basehp:35
よって表示結果は
35:35

【表示結果】
35:25
右の値が変わっていないのは、healをする前に、tの初期値にbaseHpの25が入っているから。
35:35
右の値が変わっているのは、healの後に表示しているから